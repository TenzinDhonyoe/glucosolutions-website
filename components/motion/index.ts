// Scroll-motion toolkit. Reusable, reduced-motion-aware primitives that give the
// site its Titan-style choreography. Client components — safe to drop into the
// (server-rendered) home sections as wrappers around their content.

export { SmoothScroll } from "./SmoothScroll";
export { Reveal, Stagger, StaggerItem, DrawLine } from "./Reveal";
export type { RevealVariant } from "./Reveal";
export { CountUp } from "./CountUp";
export { Parallax } from "./Parallax";
