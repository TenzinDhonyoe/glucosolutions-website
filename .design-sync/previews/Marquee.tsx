import { Marquee } from "gluco_website";
export const Default = () => (
  <div style={{ background: "#F6F1E6", paddingBlock: 24 }}>
    <Marquee items={["No needles", "No fingersticks", "No consumables", "Weekly charging", "Reverse prediabetes"]} />
  </div>
);
